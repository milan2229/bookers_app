<!DOCTYPE html>
<html>
 <h6><%= notice %></h6>
<h2>Books</h2>

<body>
  
<table>
  <thead>
    <tr>
      <th>Title</th> 
      <th>Body</th>
    </tr>
  </thead>
  <tbody>
    <% @books.each do |book| %>
    <tr>
      <td><%= book.title %></td>
      <td><%= book.body %></td>
      <td><%= link_to "Show", book_path(book.id) %></td>
      <td><%= link_to "Edit", edit_book_path(book.id) %></td>
      <td><%= link_to "Destroy", book_path(book.id), method: :delete, "data-confirm" => "Are you sure？" %></td>
    </tr>
    <% end %>
  </tbody>
</table>

<h3>New book</h3>
<% if @book.errors.any?%>
  <%= @book.errors.count %>errors prohibited this book from being saved:<br>
  <% @book.errors.full_messages.each do |message| %><br>
    <%= message%>
  <% end %>
<% end %>  
<a><%= form_with model:@book, local:true do |f| %></a>
  <h4>Title</h4><a><%= f.text_field :title %></a>
  <h4>Body</h4><a><%= f.text_area :body %></a>
  <br><%= f.submit 'Create Book' %>
<% end %>

</body>

</html>